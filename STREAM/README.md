# STREAM

STREAM is the de facto industry standard benchmark for measuring sustained memory bandwidth.

Example run:
```bash
# From the STREAM/ dir
sbatchman init
sbatchman campaign-tui
# Then input `campaign.yaml` and
# - On Dalek:   `dalek-mono-bpi-f3`
# - On BSC-HCA: `bsc-hca-pioneer`
```