apps:
  - name: STREAM
    systems: ['dalek-mono-sip-k3', 'dalek-mono-bpi-f3']
    dir: .
    blocking: true

    steps:
      - name: compile
        jobs: compile.yaml
        on_fails: terminate

      - name: experiments_performance
        jobs: jobs.yaml
        on_fails: continue

      - name: experiments_energy
        jobs: jobs_energy.yaml
        on_fails: continue