sequential: true

variables:
  gpus: [8]
  experiment: "experiments/"

  walltime: [600] # seconds

jobs:
  - config: "{gpus}_gpus"
    config_jobs:
      - tag: "{experiment_filename}"
        command: "python workerpool_scheduler.py --device-upperbound {gpus} --walltime {walltime} -p {experiment}"
